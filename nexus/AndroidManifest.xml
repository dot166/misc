<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.github.dot166.nexus">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- System permission to host activities -->
    <uses-permission android:name="android.permission.ACTIVITY_EMBEDDING"/>
    <!-- System permission to use internal system windows -->
    <uses-permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity android:name="io.github.dot166.nexus.PreferenceActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name="io.github.dot166.nexus.DefaultStub"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Wallpaper.NoTitleBar">
            <intent-filter>
                <action android:name="io.github.dot166.nexus.WINDOW_OVERLAY_PROVIDER"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <service
            android:name=".MinusOneService"
            android:enabled="true"
            android:exported="true"
            android:process="io.github.dot166.nexus"
            android:label="@string/app_name"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.android.launcher3.WINDOW_OVERLAY" />
                <data android:scheme="app" />
            </intent-filter>

            <meta-data
                android:name="service.api.version"
                android:value="7" />
        </service>

    </application>

</manifest>