[package]
name = "downloader"
version = "0.1.0"
edition = "2024"

[dependencies]
dirs = "4.0"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }


